{
  "name": "bulletin-board",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ bulletin-board",
    "create-local": "graph create --node http://localhost:8020/ bulletin-board",
    "remove-local": "graph remove --node http://localhost:8020/ bulletin-board",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bulletin-board",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.69.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
